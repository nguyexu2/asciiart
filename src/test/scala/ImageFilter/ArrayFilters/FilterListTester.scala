package ImageFilter.ArrayFilters

import org.scalatest.FunSuite

class FilterListTester extends FunSuite {
  test("no filters") {
    assert(true)
  }

  test("1 filter 1") {
    assert(true)
  }
  test("all filters at once") {
    assert(true)
  }

  test("error in the middle") {
    assert(true)
  }

}
